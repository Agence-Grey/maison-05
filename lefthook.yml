pre-commit:
  parallel: true
  commands:
    type-check:
      run: pnpm exec tsc --noEmit
    biome-check:
      glob: "*.{js,ts,cjs,mjs,d.cts,d.mts,jsx,tsx,json}"
      run: pnpm exec biome check --staged --write
      stage_fixed: true